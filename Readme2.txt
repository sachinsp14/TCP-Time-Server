Follow the below steps to execute the file:

1: use ls to list the directories and cd to got he directory where the file is stored

2: on the terminal run make -f MakeFile2

3: execute ./TCPtimeclient (server1, server2):(time-a.nist.gov,time-b.nist.gov)

4: to remove the executable file run make -f MakeFile2 clean 
 
